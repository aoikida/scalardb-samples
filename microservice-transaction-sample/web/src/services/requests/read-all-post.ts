"use server";

import { $ } from "zx";

// export const readAllPost = async (serverId: number): Promise<string> => {
//   let commandName = "";
//   switch (serverId) {
//     case 1:
//       commandName = "GetPostFromMysql";
//       break;
//     case 2:
//       commandName = "GetPostFromCassandra";
//   }
//   const exec = await $`cd ..; ./gradlew :client:run --args="${commandName}"`;

// };
